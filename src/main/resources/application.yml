
server:
  port: 8082

# config mysql
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/finelunchdb?createDatabaseIfNotExist=true
    username: root
    password: 'mypassword'
    driver-class-name: com.mysql.cj.jdbc.Driver